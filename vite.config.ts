// Minimal TypeScript config file to satisfy tsconfig.node.json
// The actual Vite config is in vite.config.js

import { defineConfig } from "vite";

// This is just a placeholder - the real config is in vite.config.js
export default defineConfig({});